apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: myapps.example.com
spec:
  group: example.com
  names:
    categories: []
    kind: MyApp
    plural: myapps
    shortNames:
    - ma
    singular: myapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for MyAppSpec via `CustomResource`
        properties:
          spec:
            properties:
              envVars:
                additionalProperties:
                  type: string
                default: {}
                description: Optional environment variables
                type: object
              image:
                description: Image to deploy
                pattern: ^[a-z0-9-./]+:[a-z0-9.-]+$
                type: string
              replicas:
                description: Number of replicas desired
                format: int32
                maximum: 100.0
                minimum: 1.0
                type: integer
              resources:
                description: Resource requirements
                nullable: true
                properties:
                  cpu:
                    type: string
                  memory:
                    type: string
                required:
                - cpu
                - memory
                type: object
              scheduling:
                description: Advanced scheduling configuration
                nullable: true
                properties:
                  nodeSelector:
                    additionalProperties:
                      type: string
                    default: {}
                    description: Node selection preferences
                    type: object
                  priorityClass:
                    description: Priority class for pod scheduling
                    nullable: true
                    type: string
                  schedulerName:
                    description: Scheduler name (for custom schedulers)
                    nullable: true
                    type: string
                type: object
            required:
            - image
            - replicas
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                description: Conditions tracking various aspects of the resource
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastUpdated:
                description: Last update timestamp
                nullable: true
                type: string
              observedGeneration:
                description: Observed generation
                format: int64
                nullable: true
                type: integer
              state:
                description: Current state of the application
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: MyApp
        type: object
    served: true
    storage: true
    subresources:
      status: {}
